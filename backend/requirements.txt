# Core LangChain (no openai dependency)
langchain
langchain-community
langchain-huggingface

# Local embeddings — no API key required
sentence-transformers

# Vector store
chromadb

# Sparse retrieval (BM25 hybrid RAG)
rank-bm25

# LLM answer generation + KB article drafting (free tier)
groq

# API server
fastapi
uvicorn[standard]

# NOTE: do NOT pin starlette>=1.2.0 here — installed fastapi (0.116.x)
# requires starlette<0.48, so that pin forces a major FastAPI upgrade or a
# resolver failure. TestClient works on the current stack via plain httpx.

# Environment config
python-dotenv

# Lets pydantic evaluate modern type syntax (str | None) on Python 3.9
eval_type_backport

# HTTP client — required by FastAPI/Starlette TestClient.
# (On starlette >= 1.2 the testclient prefers httpx2; on our 0.47.x stack
# plain httpx is the correct dependency. Both test suites pass with httpx.)
httpx
